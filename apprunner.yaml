version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.10
  command: gunicorn --bind 0.0.0.0:8080 --workers 1 --timeout 120 app:server
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
